分散ジョブ実行では安全性が最優先である。
SSH ホスト鍵の確認を怠らず、StrictHostKeyChecking=no を既定にしてはならない。
各タスクは専用の作業ディレクトリで実行し、ユーザーの元ファイルを直接変更しない。
成果物は一時名で転送し、checksum を確認してから原子的に正式名へ改名する。
機密文書は明示的な許可がない限り外部 AI API へ送信してはならない。
失敗は分類し、再試行可能なものだけを別ノードで再実行する。
